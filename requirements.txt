# NetMood Analyzer - 실시간 시스템 의존성
#
# Windows: netifaces 등이 Visual C++ Build Tools 없이 빌드 실패할 수 있음.
# API 서버·DB만 필요하면: py -3 -m pip install -r requirements-api.txt
#
# 핵심 데이터 처리
pandas>=1.5.0
numpy>=1.21.0

# 웹 프레임워크
Flask>=2.0.0
Flask-CORS>=3.0.0
requests>=2.31.0

# PostgreSQL (세션·메트릭 저장, DATABASE_URL 설정 시)
psycopg[binary]>=3.1.0

# 실시간 통신
websockets>=10.0

# 패킷 캡처 (크로스 플랫폼)
scapy>=2.4.5
psutil>=5.8.0

# 보안 및 암호화
cryptography>=3.4.8

# 시스템 모니터링
netifaces>=0.11.0

# 개발 및 테스트 (선택적)
pytest>=6.0.0
pytest-asyncio>=0.18.0

# 로깅 및 모니터링
colorlog>=6.6.0

# 데이터 직렬화
msgpack>=1.0.0

# 성능 최적화
uvloop>=0.16.0; sys_platform != "win32"

